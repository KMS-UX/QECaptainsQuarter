Rarity-framed inventory slot + rarity label chip. Real pixel icons go in via `image`; `icon` takes a glyph stand-in until sprites are provided.

```jsx
<ItemSlot rarity="epic" count={3}/>
<ItemSlot rarity="legendary" quality="+15" selected/>
<ItemSlot empty/>
<RarityTag rarity="mythic"/>
```
